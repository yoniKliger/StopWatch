import React from 'react';

const WatchControls = props => {
    static 
    return (
        <div>
            <button onClick = {() => handleStart()}>Start</button>
            <button>Stop</button>
            <button>Reset</button>
        </div>
    );
};

export default WatchControls;